# Development-only scripts

These scripts preserve historical development workflows. They are not release
entry points and may depend on source files or tools from the commit where they
were introduced. Use `scripts/check_v21_pv6_compile.sh` and
`scripts/rebuild_v21_pv6.sh` for the final architecture.
